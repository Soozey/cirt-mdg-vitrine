/**
 * Firebase client placeholder. Frontend-only mock — no real SDK loaded.
 */
export const firebase = {
  initialized: false,
  config: {
    apiKey: "MOCK",
    authDomain: "mock.firebaseapp.com",
    projectId: "mock-project",
  },
};